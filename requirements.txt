requests>=2.32.0
beautifulsoup4>=4.12.3
pandas>=2.2.0

# API + worker (B6)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0

# AuthInfra baseline detector (optional for data_helpers; required for detector-infer)
transformers>=4.30.0
torch>=2.0.0

# Gemini adapter (B7) — keys from api_keys.md only, never logged
google-generativeai>=0.8.0
